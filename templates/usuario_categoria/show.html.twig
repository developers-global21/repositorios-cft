{% extends 'base.html.twig' %}

{% block title %}UsuarioCategoria{% endblock %}

{% block body %}
    <h1>UsuarioCategoria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario_categorium.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_usuario_categoria_index') }}">back to list</a>

    <a href="{{ path('app_usuario_categoria_edit', {'id': usuario_categorium.id}) }}">edit</a>

    {{ include('usuario_categoria/_delete_form.html.twig') }}
{% endblock %}
