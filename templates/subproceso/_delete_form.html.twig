<form method="post" action="{{ path('app_subproceso_delete', {'id': subproceso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subproceso.id) }}">
    <button class="btn">Delete</button>
</form>
